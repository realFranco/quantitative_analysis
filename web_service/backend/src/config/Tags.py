class Tags:
    archi = 'Route to handle services related with Archi.'
    parser = 'Route to handle parser services from external logs into current application.'
    result = 'Route to handle computing results objects to expose into the UI.'
    redirection = 'Routes to handle content redirection.'
